functions:
  - id: agg.join
    kind: reduce
    params: [{ name: source_value, dtype: string }]
    dtype:
      rule: scalar
      kind: string
    reduction_strategy: first_element
    aliases: ["join"]