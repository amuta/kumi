functions:
  - id: core.tuple
    kind: constructor
    params: [{ name: elements, variadic: true }]
    dtype: "tuple(types(elements))"
# length is probably in the wrong place 
  - id: core.length
    kind: elementwise
    params: [{ name: collection }]
    dtype: "integer"