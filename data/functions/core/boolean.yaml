functions:
  - id: core.and
    kind: elementwise
    params: [{ name: left_operand }, { name: right_operand }]
    dtype:
      rule: scalar
      kind: boolean
    aliases: ["and", "&"]

  - id: core.or
    kind: elementwise
    params: [{ name: left_operand }, { name: right_operand }]
    dtype:
      rule: scalar
      kind: boolean
    aliases: ["or", "|"]

  - id: core.not
    kind: elementwise
    params: [{ name: operand }]
    dtype:
      rule: scalar
      kind: boolean
    aliases: ["not", "!"]