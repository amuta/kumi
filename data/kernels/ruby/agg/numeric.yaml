kernels:
  - id: agg.sum:ruby:v1
    fn: agg.sum
    impl:
      init:     "Kumi::Kernels::Agg.zero"   # neutral element for dtype
      step:     "Kumi::Kernels::Agg.sum"    # acc, val -> acc'
      finalize: "Kumi::Kernels::Agg.id"     # acc -> out