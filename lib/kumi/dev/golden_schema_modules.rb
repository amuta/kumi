# frozen_string_literal: true

# Base module for golden schema imports
# Shared schemas from golden/_shared/ should be required separately
# before using any schemas that import from GoldenSchemas

module GoldenSchemas
end
