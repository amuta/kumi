# frozen_string_literal: true

# Minimal public builder used by app initializers to register custom functions.

module Kumi
  # Empty module to satisfy zeitwerk expectations
  module FunctionBuilder
  end
end
